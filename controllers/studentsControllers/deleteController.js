// import { Student } from "../../mongoDB/models/models";

// export default function deleteController(req, res) {

//     await Student.delete({ id: _id }, (err) => {
//         if (!err) {
//             res.status(200).json({ messege: "Delete Student Successfully" });
//         } else {
//             res.status().json({ error: "There was a server side Error" });
//         }
//     });

// }
